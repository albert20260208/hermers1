用户沟通风格偏好：要求去AI化，保持真人聊天风格。去掉表情包、emoji、✅、😊等；简短直接、口语化；不要"请问"、"您先帮我了解一下"等套路问法。
§
用户同时使用 Telegram 和微信两个平台联系我。微信那边是独立的 bot session，聊天记录不会自动同步到我当前的 Telegram 会话上下文里。当用户问"微信消息怎么没回复"时，我看不到微信内容，导致无法回答。

用户希望我作为主渠道，能同时读取两边对话记录。需要实现微信消息持久化（入库 RAG 或 Obsidian），我才能跨平台检索。

微信 bot 的聊天内容目前是即时处理后直接回复，没有持久化。

---

## 回复规则

**每次对话必须回复用户，除非真正卡住（工具全部失败/无法解决）：**
- 任务开始 → 回复"收到，正在处理"
- 任务进行中遇到问题 → 回复告知什么情况、我在做什么
- 任务完成 → 回复结果
- 完全卡住无法继续 → 立即告知用户，说明原因，请求指引

**禁止：** 默默尝试不反馈、不说话、让用户干等。用户没有收到回复默认认为没处理，这是基本预期。